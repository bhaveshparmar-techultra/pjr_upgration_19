<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_company_inherit_other_partner_form" model="ir.ui.view">
            <field name="model">res.company</field>
            <field name="name">view.company.l10n.cl.edi.form</field>
            <field name="inherit_id" ref="base.view_company_form" />
            <field name="arch" type="xml">
                <!-- Odoo 19: favicon field moved, using partner_id instead -->
                <field name="partner_id" position="after">
                    <field name="other_partner" string="Other Partner (for Reports)"/>
                </field>
            </field>
        </record>
    </data>
</odoo>
